third.txtには何も入力していなかったので今入力した。

diffに関する追記で変更差分を確認する

git diffの動作確認用に追記

git diff -stagedの動作確認用に追記

mvコマンドにはgit addが含まれている

git reset HEADはステージングにaddした内容を取り消す。ワークツリーの状態は変わらない。

git commit --amendは直前のコミットをやり直す。


